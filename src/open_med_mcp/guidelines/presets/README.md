Preset guidelines. Each file is Markdown with YAML front matter (name, title, tags, modalities,
tasks, models, summary). Copy one into `omm_guidelines/` in your workspace (or a directory in
`OMM_GUIDELINE_DIRS`) and edit it to customise the protocol; a file with the same `name` overrides
the preset.
